[project]
name = "compare-ifsf-amex"
version = "0.1.0"
description = "ISO 8583 message comparison tool for AMEX/IFSF message formats"
readme = "README.md"
requires-python = ">=3.13.7"
dependencies = [
    "pyiso8583>=3.0.0",
    "pyyaml>=6.0",
    "streamlit>=1.30.0",
    "pandas>=2.0.0",
]
