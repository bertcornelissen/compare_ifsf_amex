[project]
name = "compare-ifsf-amex"
version = "0.1.0"
description = "ISO 8583 Message Comparison Tool"
readme = "README.md"
requires-python = ">=3.13.7"
dependencies = [
    "pyiso8583>=4.0.1",
    "pyyaml>=6.0",
    "streamlit>=1.53.0",
]

[project.scripts]
iso8583-compare = "main:main"
